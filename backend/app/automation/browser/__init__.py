"""Browser management package.

This package defines classes responsible for launching and interacting
with a Playwright browser. `BrowserManager` manages lifecycle and
screenshot capture, while `AuthManager` handles initial logins and
persisting authentication state.
"""